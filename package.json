{
  "name": "sparkxrstart",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "copy-scene-configs": "node scripts/copy-scene-configs.js",
    "dev": "npm run copy-scene-configs && vite",
    "prebuild": "npm run copy-scene-configs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.12.0"
  }
}
